# Backlog Inbox

Raw ideas not yet promoted to GitHub Issues.

## Inbox

- [x] [feat] [high] **Agent/MCP-guided first-run setup**. Detect missing Vercel/Upstash/Telegram/model config and walk the cloner through official CLIs/integrations (and MCP tools when available) without pasting secrets into model context or requiring template-maintainer credentials.
- [ ] [feat] [medium] **Platform-owned agent creator**. Build a separate web application that consumes a released template and owns provisioning, billing, isolation, lifecycle, and export for non-technical users.
- [ ] [feat] [medium] **Platform Telegram agent creation**. Let non-technical users create and manage an agent through a guided Telegram conversation after the hosted web flow is validated (distinct from V1's user-owned Telegram chat channel).
- [ ] [feat] [low] **Agent template gallery**. Offer opinionated starter personalities and use cases without changing the underlying runtime.
- [ ] [feat] [low] **Markdown-only auto-apply**. After V1 apply-loop metrics settle, optionally auto-apply narrowly scoped markdown skill edits without production confirmation while keeping code promotion gated.
